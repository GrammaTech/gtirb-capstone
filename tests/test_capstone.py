import unittest


class CapstoneTest(unittest.TestCase):
    def test_1_invocation(self):
        # test_gtirb = "tests/hello.v1.gtirb"
        # baseline_output = test_gtirb + ".functions"
        # test_output_fn = "test.tmp"

        # args = ["python3", "-m", "gtirb_functions", test_gtirb]
        # with open(test_output_fn, "w+") as redir:
        #     ec = subprocess.call(args, stdout=redir)
        #     self.assertEqual(ec, 0, msg="function list generated")
        # args = ["diff", test_output_fn, baseline_output]
        # ec = subprocess.call(args)
        # self.assertEqual(ec, 0)
        return True
